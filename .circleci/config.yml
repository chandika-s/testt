version: 2.1
jobs:
  testjob:
    docker:
      - image: alpine:3.15
    steps:
      - checkout
      - run:
          name: The First Step
          command: |
            echo 'Hello World!'
            echo 'This is the delivery pipeline'
workflows:
  testwf:
    jobs:
      - testjob:
          filters:
            tags:
              only: /^test.*/
            branches:
              ignore: /.*/
              
        
